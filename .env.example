# Environment Variables for Pipedrive Migration Tool
# Copy this file to .env for local development (never commit .env!)

# Make.com API Token (only needed for CLI mode with --id flag)
# Get from: https://www.make.com/en/api-documentation/authentication
MAKE_API_TOKEN=your-make-api-token-here

# Default Pipedrive OAuth Connection ID (optional)
# Users can override this in the web interface
PIPEDRIVE_OAUTH_CONN_ID=4683394

# Default Pipedrive OAuth Connection Label (optional)
PIPEDRIVE_OAUTH_CONN_LABEL=My Pipedrive OAuth connection
